{
  "name": "breathing-timer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check storage.js && node --check ui-utils.js && node --check model.js && node --check script.js && node --check sw.js"
  }
}
